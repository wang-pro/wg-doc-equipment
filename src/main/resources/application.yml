server:
  port: 9006
  servlet:
    context-path: /doc-equipment-api

spring:
  application:
    name: doc-equipment
  profiles:
    active: dev
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  file: logs.log

